import React from "react";

const Input = () => {
  return (
    <input
      type="text"
      id="fullname"
      className="input"
      placeholder="Enter your fullname"
    />
  );
};

export default Input;
